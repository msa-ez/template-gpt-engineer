Please implement for this Event Storming Model:

{{{contexts.json}}}


I want to utilize following techniques:

- clean architecture
- spring-boot3
- spring-data-rest
- JPA (must use package jakarta.persistence.* instead of javax.persistence.*)
- H2 DB
- Lombok
- Kafka with spring-cloud-streams library 
- JUnit Test


SELF-CRITICISM: 
 - when you create pom.xml, USE correct parent project to spring boot 3
 - when you create pom.xml, you MUST specify the version: 3.1.3 ONLY for the spring-cloud-streams
 - Please generate start from pom.xml


when you create execution shell (run.sh), please add '--no-transfer-progress' option not to output transfer progress if there's mvn command:

```
mvn [command] --no-transfer-progress
```


<function>

var cache = []
const includingKeys = ["", "_type", "fieldDescriptors", "className", "displayName", "events", "aggregateRoot", "aggregates", "policies", "commands", "actors", "name", "examples", "given", "when", "then", "value", "type"]
const cycleAvoid = (key, value, path = '') => {
    // examples 내부의 모든 데이터를 유지
    if (path.includes('examples')) {
        if (typeof value === 'object' && value !== null) {
            if (cache.includes(value)) return '[Circular]';
            cache.push(value);
            
            if (Array.isArray(value)) {
                return value.map((item, index) => cycleAvoid(index, item, `${path}.${key}`));
            } else {
                const newObj = {};
                for (let i in value) {
                    if (Object.prototype.hasOwnProperty.call(value, i)) {
                        newObj[i] = cycleAvoid(i, value[i], `${path}.${key}`);
                    }
                }
                return newObj;
            }
        }
        return value;
    }

    // includingKeys에 포함되지 않은 키는 제외
    if (isNaN(key) && !includingKeys.includes(key)) return undefined;

    if (key === "_type" && typeof value === 'string' && value.indexOf('.') > 1) {
        value = value.split(".").pop();
    }

    if (typeof value === 'object' && value !== null) {
        if (cache.includes(value)) return undefined;
        cache.push(value);

        if (Array.isArray(value)) {
            return value.map((item, index) => cycleAvoid(index, item, `${path}.${key}`)).filter(item => item !== undefined);
        } else {
            const newObj = {};
            for (let i in value) {
                if (Object.prototype.hasOwnProperty.call(value, i)) {
                    const subValue = cycleAvoid(i, value[i], `${path}.${key}`);
                    if (subValue !== undefined) {
                        newObj[i] = subValue;
                    }
                }
            }
            return Object.keys(newObj).length > 0 ? newObj : undefined;
        }
    }
    return value;
};

// 입력 객체의 복사본을 만들어 처리
const inputCopy = JSON.parse(JSON.stringify(this));
this.contexts.json = JSON.stringify(inputCopy, (key, value) => cycleAvoid(key, value, ''));


</function>
